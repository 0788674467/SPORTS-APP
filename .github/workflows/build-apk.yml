name: Test & Build APK

on:
  # Allow manual trigger from GitHub Actions tab
  workflow_dispatch:
  
  # Auto-build on push to main/master branch
  push:
    branches: [ main, master ]
    paths:
      - 'frontend/**'
      - '.github/workflows/build-apk.yml'

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
      
      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.4'
          channel: 'stable'
          cache: true
      
      - name: 📦 Get dependencies
        working-directory: frontend
        run: flutter pub get
      
      - name: 🧪 Run unit, widget & integration tests with coverage
        working-directory: frontend
        run: flutter test --coverage
      
      - name: 📊 Show test summary
        working-directory: frontend
        run: |
          echo "✅ All tests passed"
          lines=$(wc -l < coverage/lcov.info 2>/dev/null || echo 0)
          echo "📈 Coverage records: $lines lines"
      
      - name: 📤 Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: test-coverage
          path: frontend/coverage/
          retention-days: 7
          if-no-files-found: ignore
      
      - name: 🔨 Build APK
        working-directory: frontend
        run: flutter build apk --release

      - name: 📊 Get APK info
        working-directory: frontend
        run: |
          ls -lh build/app/outputs/flutter-apk/
          echo "APK Size: $(du -h build/app/outputs/flutter-apk/app-release.apk | cut -f1)"

      - name: 📤 Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: frontend/build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30

      - name: 📤 Upload Split APKs (if exist)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: split-apks
          path: frontend/build/app/outputs/flutter-apk/app-*-release.apk
          retention-days: 30
          if-no-files-found: ignore
